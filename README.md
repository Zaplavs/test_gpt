# test_gpt

Простой тестовый сайт на HTML и CSS с маленьким JavaScript-интерактивом.

## Файлы
- `index.html` — разметка сайта
- `style.css` — стили

Откройте `index.html` в браузере, чтобы посмотреть сайт локально.
